{
  "name": "clawtrader-frontend-legacy",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "dev:legacy": "echo '⚠️  This is the LEGACY UI (port 3000). The Arena UI is at service/arena (port 3100). Run: cd service/arena && npm run dev' && vite",
    "build": "tsc && vite build",
    "postbuild": "chmod -R a+rX dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "ethers": "^6.10.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.21.0",
    "recharts": "^3.8.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
